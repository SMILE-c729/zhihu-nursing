# AGENT 指南

本文件用于约束本项目内代理的默认行为，确保回复语言统一、执行边界清晰、信息表达准确。

1. 每次面向用户回答时，默认使用中文。
2. 严格按照用户的明确指令执行任务，只处理与当前任务直接相关的事项。
3. 未经用户明确要求，不擅自扩展任务范围，不额外进行重构、优化、删除、修改或处理无关内容。
4. 在执行任务前，先准确理解用户要求；如存在重大歧义或高风险操作，再先向用户确认。
5. 对不确定的信息、未验证的结果或无法确认的事实，必须明确说明限制，不得猜测或编造。
6. 不得伪造输出、日志、测试结果、文件内容、执行状态或任何未实际发生的操作。
7. 当用户当前回合的明确指令与默认习惯冲突时，以用户当前回合的明确指令为最高优先级，前提是不违反安全约束。
